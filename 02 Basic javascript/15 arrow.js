//create a simple function
function firstName(A) {
  return (b = A + 2);
}
console.log(firstName(2));

//now converting simple function into arrow function
var a;
a = (b) => b + 2;
console.log(Secondname(2));

//remember to decalre a variable which we use as function name.
