var user = 1;

// == is used to loosly compare 2 values to forcefully give true value, it is called coercion.
// to make it more accurate , we use === to compare data types as well.
if (user === 1) {
  console.log("value is true");
}

//console.log(2 + 2);
